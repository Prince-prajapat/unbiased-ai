// app/dashboard/page.tsx — Main Dashboard
// TODO: Implement in next phase

/**
 * Main dashboard showing list of all audit reports.
 * Reads from Firestore in real-time.
 * Features:
 *  - Report cards with fairness score + risk level
 *  - Link to individual report pages
 *  - "New Audit" button → /upload
 */

export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard — Your Audit Reports</h1>
      {/* TODO: Firestore-backed report list */}
    </main>
  )
}

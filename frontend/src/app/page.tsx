// app/page.tsx — Landing / Login Page
// TODO: Implement in next phase

/**
 * Landing page with Firebase Email/Password login.
 * On successful auth → redirect to /dashboard
 */

export default function HomePage() {
  return (
    <main>
      <h1>Unbiased AI — Login</h1>
      {/* TODO: Firebase login form */}
    </main>
  )
}

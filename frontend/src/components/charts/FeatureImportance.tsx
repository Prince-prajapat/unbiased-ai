// components/charts/FeatureImportance.tsx
// TODO: Implement in next phase

/**
 * Horizontal bar chart showing which features most influenced
 * the model's decisions (feature importance / SHAP values).
 *
 * Props:
 *  - features: Array<{ name: string, importance: number }>
 *
 * Visual:
 *  - Sorted descending by importance
 *  - Color-coded: red if correlated with protected attribute
 *  - Tooltip explaining each feature's role
 */

export default function FeatureImportanceChart({ features }: any) {
  // TODO: implement
  return <div>FeatureImportanceChart</div>
}
